const INITIAL_STATE = {
    projetos: [],
};

function projeto(state = INITIAL_STATE, action) {
    switch (action.type) {
        case '@projeto/ALL': {
            ///
            break;
        }
        default: 
            return state;
    }
} 

export default projeto;